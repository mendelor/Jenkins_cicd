pipeline {
    agent any 
    options { disableConcurrentBuilds() }
    stages { 
      stage ("stage1") {
        steps {
          echo "Build ID: ${env.BUILD_ID}, Jenkins URL: ${env.JENKINS_URL}" 
        
           }      
         }
      }
   }
